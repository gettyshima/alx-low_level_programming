#include "main.h"
/**
  *add - adds two integer and returns the result
  *@x: first integer
  *@y: second integer
  *int: values entered as int
  *
  *Return: the result
  */
int add(int x, int y)
{
	int sum;

	sum = int + int;
	return (sum);
}

