#include "main.h"

/**
  *_strlen - returns thr lenhht of a string
  *@s: string to be counted
  *
  *Return: the lenght of the string
  */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
